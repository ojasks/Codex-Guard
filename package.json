{
  "name": "codex-guard",
  "version": "1.0.0",
  "description": "Codex Guard: an AI code reviewer for engineering standards, built on Cloudflare Agents, Workflows and Workers AI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc -p . && tsc -p test",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": {
    "agents": "^0.24.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "wrangler": "^4.135.0"
  },
  "private": true,
  "type": "module"
}
